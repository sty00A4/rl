features
    assert node
    dictionary data type
    stack data type
    queue data type
    set data type
    switch node
fixes
    weird error display (has to do with how node position range is created)