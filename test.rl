object math
	pi is 3.141592653589793
	func floor(n:Number) -> Number  		n // 1 end
	func ceil(n:Number) -> Number   		n // 1 if n - n // 1 = 0 else n // 1 + 1 end
	func max(a:Number, b:Number) -> Number 	a if a > b else b end
	func min(a:Number, b:Number) -> Number 	a if a < b else b end
end
